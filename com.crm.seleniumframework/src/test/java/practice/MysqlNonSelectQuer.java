package practice;

import java.sql.Connection;
import java.sql.Statement;

import com.mysql.jdbc.Driver;

public class MysqlNonSelectQuer {
	/*String key="webdriver.chrome.driver";
	String value="./chromedriver.exe";
	System.setProperty(key,value);

Driver driverref=new Driver();
DriverManager.registerDriver(driverref);
Connection conn=DriverManager.getConnection("jdbc:Mysql//localhost/3306/testyantra","root", "root");

Statement sta=conn.createStatement();

String query1="insert into details"('first_name','Last_Name')values('muhil','arasi)
 int result= sta.executeUpdate(query1);

if(result==1)
{
System.out.println("User created");
}
else
{
System.out.println("User created");
}

conn.close();*/
}
